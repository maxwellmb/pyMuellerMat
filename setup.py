from setuptools import setup, find_packages

#setup.py for pyMuellerMat

setup(
	name='pyMuellerMat',
	version='0.1', 
	packages=find_packages(),
	license="TBD",
	long_description=open('README.md').read()
	)